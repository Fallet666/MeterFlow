services:
  db:
    image: postgres:16
    restart: unless-stopped
    environment:
      POSTGRES_DB: meterflow
      POSTGRES_USER: meterflow
      POSTGRES_PASSWORD: meterflow
    volumes:
      - postgres_data:/var/lib/postgresql/data
    ports:
      - "5432:5432"

  backend:
    build:
      context: ./backend
      target: runtime
    env_file:
      - ./backend/.env.example
    environment:
      DB_ENGINE: postgres
      POSTGRES_DB: meterflow
      POSTGRES_USER: meterflow
      POSTGRES_PASSWORD: meterflow
      POSTGRES_HOST: db
    volumes:
      - ./backend:/app
    ports:
      - "8000:8000"
    depends_on:
      - db

  frontend:
    build:
      context: ./frontend
      args:
        VITE_API_URL: ${FRONTEND_VITE_API_URL:-/api/}
    environment:
      VITE_API_URL: /api/
    volumes:
      - ./frontend:/app
      - /app/node_modules
    ports:
      - "5173:80"
    depends_on:
      - backend

  backend-tests:
    build:
      context: ./backend
      target: test
    profiles: ["test"]
    env_file:
      - ./backend/.env.example
    environment:
      DB_ENGINE: postgres
      POSTGRES_DB: meterflow
      POSTGRES_USER: meterflow
      POSTGRES_PASSWORD: meterflow
      POSTGRES_HOST: db
    volumes:
      - tests-sync:/tests-sync
    command: >
      sh -c "rm -f /tests-sync/backend_done /tests-sync/frontend_done &&
      uv run --no-sync pytest &&
      touch /tests-sync/backend_done &&
      while [ ! -f /tests-sync/frontend_done ]; do
        sleep 1;
      done"
    depends_on:
      - db

  frontend-tests:
    image: node:20-alpine
    profiles: ["test"]
    working_dir: /app
    environment:
      VITE_API_URL: http://localhost:8000/api/
      CI: "true"
    depends_on:
      - backend-tests
    volumes:
      - ./frontend:/app
      - frontend_node_modules:/app/node_modules
      - npm_cache:/root/.npm
      - tests-sync:/tests-sync
    command: >
      sh -c "rm -f /tests-sync/backend_done /tests-sync/frontend_done &&
      npm ci --prefer-offline --no-audit --fund=false &&
      npm test &&
      touch /tests-sync/frontend_done &&
      while [ ! -f /tests-sync/backend_done ]; do
        sleep 1;
      done"

volumes:
  postgres_data:
  tests-sync:
  frontend_node_modules:
  npm_cache:
